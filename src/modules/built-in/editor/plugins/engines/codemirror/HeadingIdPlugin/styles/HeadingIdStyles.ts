/**
 * HeadingIdPlugin 样式定义
 * 
 * 标题 ID 标记 {#xxx} 在编辑器中的视觉样式
 */
export const HEADING_ID_CSS = `
/* 标题 ID 标记在可见时的淡化样式 */
.cm-heading-id-mark {
    opacity: 0.4;
    font-size: 0.85em;
    color: var(--text-muted, #888);
}
`;
